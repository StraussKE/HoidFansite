@model UserStory

@{
    Layout = "_Layout";
    ViewBag.Title = "Reviews: " + Model.Title;
}

<!DOCTYPE html>

<html>
<head>
    <!-- _Layout -->
</head>
<body>
    <div class="panel-body">
        <h2>Fanfiction</h2>
        <table class="table table-sm table-striped table-bordered">
            <thead>
                <tr>
                    <th>Review Title</th>
                    <th>Reviewer</th>
                    <th>Rating</th>
                </tr>
            </thead>
            <tbody>
                @foreach (HoidFansite.Models.UserReview r in Model.Reviews)
                 {
                    <tr>
                        <td>@r.Title</td>
                        <td>@r.Author</td>
                        <td>@r.Rating</td>
                    </tr>
                    <tr>
                        <td colspan="3">@r.Review</td>
                    </tr>
                    <tr>
                        <td colspan="3"></td>
                    </tr>
                 }
            </tbody>
        </table>
    </div>
</body>
</html>